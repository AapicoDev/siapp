"use client";

import { TickCircle } from "iconsax-react";

export function CheckCircle() {
  return (
    <TickCircle
      variant="Bold"
      style={{
        marginTop: "14px",
        marginRight: "8px",
        color: "#A7E5A6",
        backgroundColor: "white",
        borderRadius: "9999px",
      }}
    />
  );
}
