"use client";

import { CloseCircle, TickCircle } from "iconsax-react";

export function CrossCircle() {
  return (
    <CloseCircle
      variant="Bold"
      style={{
        marginTop: "14px",
        marginRight: "8px",
        color: "#F66262",
        backgroundColor: "white",
        borderRadius: "9999px",
      }}
    />
  );
}
